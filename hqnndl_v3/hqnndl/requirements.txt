# ══════════════════════════════════════════════════════════════════
# HQNNDL v3.0 — Python Dependencies
# ══════════════════════════════════════════════════════════════════
# Install:  pip install -r requirements.txt
# Python:   3.9+  (tested on 3.9, 3.10, 3.11, 3.12)
# ──────────────────────────────────────────────────────────────────

# ── Core (required) ───────────────────────────────────────────────
flask>=2.3,<4.0
numpy>=1.24,<3.0
pillow>=10.0,<12.0

# ── Optional — PyTorch CNN backbone (ResNet-18 / EfficientNet) ────
# Uncomment to enable real CNN feature extraction.
# Then set USE_TORCH = True in backend/cnn_features.py
#
# torch>=2.0
# torchvision>=0.15

# ── Optional — PennyLane quantum framework ────────────────────────
# Uncomment to replace NumPy VQC simulator with PennyLane.
# Then set USE_PENNYLANE = True in backend/quantum_model.py
#
# pennylane>=0.35

# ── Optional — Qiskit quantum framework ──────────────────────────
# Uncomment to replace NumPy VQC simulator with Qiskit.
# Then set USE_QISKIT = True in backend/quantum_model.py
#
# qiskit>=1.0

# ── Optional — Research / evaluation extras ───────────────────────
# Recommended for training on real OCT datasets and ablation studies.
#
# opencv-python>=4.8       # Advanced image processing
# scikit-learn>=1.3        # Metrics, SVM baseline, PCA
# pandas>=2.0              # CSV export, dataset handling
# matplotlib>=3.7          # Plotting, figure generation
# seaborn>=0.13            # ROC curves, confusion matrices
